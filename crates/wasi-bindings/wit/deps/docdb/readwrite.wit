interface readwrite {
    use types.{database, error, query};
    
    /// create.
    // create: func(db: borrow<database>, d: borrow<document>) -> result<_, error>;

    /// query.
    find: func(db: borrow<database>, s: borrow<query>) -> result<list<list<u8>>, error>;

    /// update.
    // update: func(db: borrow<database>, f: borrow<filter>, d: borrow<document>) -> result<_, error>;

    /// delete.
    // delete: func(db: borrow<database>, f: borrow<filter>) -> result<_, error>;
}